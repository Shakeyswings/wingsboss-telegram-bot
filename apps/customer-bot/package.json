{
  "name": "@wingboss/customer-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build -w @wingboss/core && tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@wingboss/core": "*",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "tsx": "^4.19.3"
  }
}
